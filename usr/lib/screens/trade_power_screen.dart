import 'package:flutter/material.dart';

class TradePowerScreen extends StatelessWidget {
  const TradePowerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Trade Power Management - Coming Soon'),
      ),
    );
  }
}